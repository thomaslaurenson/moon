# Python library with an owned CLI/app layer - code-authoring rules.
# Use when the library configures logging at its console-script entry point instead of
# leaving stack configuration to the consumer. Same rules as python-lib-code; the
# logging fragment covers the entry-point-configures / modules-only-log split.
_core
git/commits
python/style
python/types
python/docstrings
python/errors
cli/output
python/logging-stdlib
python/typecheck
