# Bash badge row

A Bash project has no language row. There is no toolchain version to show, since the scripts run on whatever bash the machine has, and bats produces no coverage figure. The CI and release rows from the badge conventions are the whole set, and a project that attaches no release assets drops the downloads badge as that fragment says.
